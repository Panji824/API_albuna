{
  "$schema": "https://railway.app/schemas/cli/v2",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "sh ./build.sh"
  },
  "deploy": {
    "startCommand": "gunicorn API_albuna.wsgi --bind 0.0.0.0:$PORT --timeout 60 --log-file -",
    "healthcheckPath": "/health/"
  }
}