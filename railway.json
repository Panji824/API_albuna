{
  "$schema": "https://railway.app/schemas/cli/v2",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "sh ./build.sh"
  },
  "deploy": {
    "startCommand": "web: gunicorn API_albuna.wsgi --bind 0.0.0.0:$PORT --log-file -",
    "healthcheckPath": "/admin/"
  }
}